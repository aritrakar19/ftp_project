import axios from 'axios';
import fs from 'fs';
import FormData from 'form-data';

(async () => {
    try {
        const token = "dummy-token"; // Not gonna work unauthenticated
        console.log("Just running script");
    } catch (e) {
        console.error(e);
    }
})();
